import 'package:flutter/material.dart';

class Acerca extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Container(
        child: Center(
          child: Text(
            "Hardccionario esta creado para las personas que desean aprender en el ambito de la electronica"
          ),
        ),
      );
  }
}